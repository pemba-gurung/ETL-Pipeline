# The Goal of this project is to extract data from a zipped HTML document, tranform the data and load the transformed data to an external API in JSON format.

# First extracts the data and load it into a pandas dataframe.

![alt text](<Screenshot 2026-02-01 at 9.24.22 PM.png>)

# After the the script groups the data by date and item,

# It shows the aggregated sales and units for each item on each day.

# Every row have an entry for order date, product, sales, and quantity.

![alt text](<Screenshot 2026-02-01 at 9.24.36 PM.png>)
# This project is built in a scalable manner, so we can pass in a dataframe as often as we need.

# Atlast, the dataframe is converted to json and post it to external API for downstream application.

![alt text](<Screenshot 2026-02-01 at 9.25.01 PM.png>)